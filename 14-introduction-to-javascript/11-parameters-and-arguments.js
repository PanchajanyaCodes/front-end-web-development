// Parameters and Arguments.

function greetUser(userName) {
	alert("Hello " + userName);
}

greetUser(prompt("Enter your name: "));

// Math.floor() function is used to round down a number.
console.log(Math.floor(4.99));
